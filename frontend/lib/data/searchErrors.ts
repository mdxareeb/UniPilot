/**
 * The retrieval domain's sanitized copy (Task 25.x), same discipline as the
 * other data modules: a raw Postgres/PostgREST message never reaches the UI.
 */

export const SEARCH_ERROR =
  "We couldn't run that search. Please try again.";
